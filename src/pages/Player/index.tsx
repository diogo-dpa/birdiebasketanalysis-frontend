import React from 'react';

import { Container } from './styles';

const Player: React.FC = () => {

    return (
        <Container>
            <h1>Player</h1>
        </Container>
    );
};

export default Player;